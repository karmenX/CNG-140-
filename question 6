#include <stdio.h>
#include <math.h>

int main() {
    float x;
     float y,f;
     printf("enter x: ");
     scanf("%1f",&x);
     y=2*x;
     f=(2* cos(sqrt(2*x)* pow(y,2)))+ ((4*x)+3)/((3* pow(x,2))+(2*y)+4)+ (sin(y));

     printf("x= %3.2f\t y=%3.2f\t f=%3.2f",x,y,f);

     return 0;
}
